# react-server-examples/todomvc

An example project for `redfin/react-server` which implements TodoMVC with redux.

For instructions on how to install and run, read the [main project README](../README.md).
