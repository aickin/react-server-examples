#!/usr/bin/env node

require("babel/register")({
	stage: 1, 
	optional: [
		"runtime",
		"utility.inlineEnvironmentVariables",
	],
});
require.extensions['.css'] = function(module, filename) {
  return module._compile("", filename);
};

require("react-server-cli")("./src/simple/routes.js");